import { TariffBenefitPipe } from './tariff-benefit.pipe';

describe('TariffBenefitPipe', () => {
  it('create an instance', () => {
    const pipe = new TariffBenefitPipe();
    expect(pipe).toBeTruthy();
  });
});
