import { TariffSpeedPipe } from './tariff-speed.pipe';

describe('TariffSpeedPipe', () => {
  it('create an instance', () => {
    const pipe = new TariffSpeedPipe();
    expect(pipe).toBeTruthy();
  });
});
